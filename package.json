{
  "name": "manapotion-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "dev": "cd example && bun dev",
    "tsup-all": "cd packages/util && npm run tsup",
    "pub": "npm run tsup-all && lerna publish"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
