{
  "name": "manapotion-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "dev": "cd example && bun dev",
    "version": "lerna version"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
