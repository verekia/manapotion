{
  "name": "manapotion-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "dev": "cd example && npm run dev",
    "pub": "npm run tsup -ws && lerna publish"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
