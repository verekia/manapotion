{
  "name": "manapotion-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "dev": "cd example && npm run dev",
    "tsup": "npm run tsup -ws",
    "publint": "npm run publint -ws",
    "pub": "npm run tsup && npm run publint && lerna publish"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
