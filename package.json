{
  "name": "manapotion-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "dev": "cd example && bun dev",
    "pub": "git diff --exit-code && cd engine && npm publish"
  }
}
